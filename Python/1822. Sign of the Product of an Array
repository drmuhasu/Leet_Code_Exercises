class Solution(object):
    def arraySign(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        sum = 1 

        for int in nums:
            sum *= int

        if sum > 0:
            return 1
        elif sum < 0:
            return -1 
        return 0
